commands:
  test:
    - "pnpm test"
    - "cd plugins/launchpad/scripts && python -m pytest -x -q"
  typecheck:
    - "pnpm typecheck"
  lint:
    - "pnpm lint"
  format:
    - "pnpm format"
  build:
    - "pnpm build"
