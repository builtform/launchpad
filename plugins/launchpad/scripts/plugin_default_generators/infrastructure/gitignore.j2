# Build outputs
build/
dist/

# Environments
.env
.env.local
.env.consultant
.env.migrate

# Logs
*.log

# OS
.DS_Store

# Python (universal — every Python tool emits these regardless of framework)
*.pyc
__pycache__/

# Misc caches
.cache/
coverage/

# Generated images
generated_images/

# LaunchPad runtime
.launchpad/audit.log
.launchpad/.bootstrap-in-progress
.launchpad/.bootstrap.lock
.launchpad/backups/
# v2.1.7 BL-364 + v2.1.8 BL-370 / BL-371 preflight runtime artifacts.
.launchpad/preflight-checklist.md
.launchpad/preflight-receipt.json
.launchpad/preflight.config.skipped
# LaunchPad scaffold tempdirs (composition + single-adapter wrapping)
.lp-tmp/

# v2.1.6 BL-350: stack-specific patterns are injected here by
# `lp_bootstrap/stack_ignore_patterns.py` based on `stacks:` in
# `.launchpad/config.yml`. The kernel template no longer hardcodes
# `node_modules/`, `.next/`, `.turbo/`, etc. — those move into the
# matching stack entry in `_ignore_patterns.py`.
# STACK_AWARE_GITIGNORE_BEGIN
# STACK_AWARE_GITIGNORE_END
