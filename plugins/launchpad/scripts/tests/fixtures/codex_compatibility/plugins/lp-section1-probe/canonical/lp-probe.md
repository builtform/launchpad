# Canonical probe

Marker: `LP_SECTION1_INSTALLED_ROOT_OK`

This file proves that a packaged skill can resolve a canonical sibling after a
local marketplace installation copies the plugin into the Codex cache.
