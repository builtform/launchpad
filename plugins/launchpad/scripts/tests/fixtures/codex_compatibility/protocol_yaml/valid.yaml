name: lp-protocol-probe
description: Strict protocol fixture.
x-launchpad:
  schema-version: 1
  component-kind: command
  direct:
    skills:
      - lp-probe-explicit
    scripts:
      - scripts/probe.py
  capabilities:
    required:
      - canonical_resource_read
    mutation: none
    interaction: none
    external-data-egress: false
    tool-profile: read_only
    fallback: inspect_only
