# LaunchPad v2.0 plugin runtime dependencies.
# Pin versions live alongside this file in `_vendor/` (PSUTIL_VERSION,
# PYYAML_VERSION) so CI and downstream installs use the same pin source.
#
# Install with:
#   pip install -r plugins/launchpad/scripts/requirements.txt
#
# Required for: /lp-scaffold-stack runtime (psutil — pid_identity), config
# loading (pyyaml — engine.py, plugin-v2-handshake-lint.py).

psutil==7.2.2
pyyaml==6.0.3
