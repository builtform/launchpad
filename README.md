# project-template
project template works as a baseline for all of my new projects. 
Option A — GitHub UI

Open the template repo → Use this template → Create a new repository.

Option B — GitHub CLI (fastest if you do this a lot)

gh repo create my-new-project --template OWNER/project-template --private --clone

That --template flag is built-in.
